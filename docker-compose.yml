services:
  mcp-crawler:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mcp-crawler
    restart: unless-stopped
    ports:
      - "4567:8021"
    command: uv run streamlit run app.py --server.port=8021 --server.address=0.0.0.0